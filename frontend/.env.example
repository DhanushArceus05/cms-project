# Base URL of the CMS backend API (Phase 2). Do not include a trailing slash.
# All API calls from this app happen in the browser, so this should stay a
# host-accessible URL even when running via Docker Compose (see root
# .env.example / ADMIN_FRONTEND_API_URL).
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
